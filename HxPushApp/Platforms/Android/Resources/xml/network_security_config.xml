<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        允许所有 http/ws 明文连接，便于测试不同服务器地址。
        生产环境建议优先使用 https/wss，再收紧这里的配置。
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
